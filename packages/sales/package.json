{
  "name": "@autostackup/sales",
  "version": "0.1.5",
  "description": "The only sales MCP you'll ever need. Built by professionals using IBM's BANT, PTC's MEDDIC, and Fortune 500 pipeline frameworks. Qualify leads in seconds, score ICP fit, forecast revenue with weighted probability, and generate personalized outreach across email, LinkedIn & SMS — all inside Claude. Stop paying $300/hr consultants for strategy Claude can run right now.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "autostackup-sales": "dist/server.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./server": {
      "import": "./dist/server.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "keywords": [
    "mcp",
    "autostackup",
    "sales",
    "crm",
    "leads",
    "pipeline"
  ],
  "mcpName": "io.github.autostackup/sales",
  "license": "MIT",
  "dependencies": {
    "@autostackup/core": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.0"
  }
}
